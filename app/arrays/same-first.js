export default function (array1, array2) {
  if (array1[0] === array2[0]) {
    const sameFirst = true;

    return sameFirst;
  }

  if (array1[0] != array2[0]) {
    const sameFirst = false;

    return sameFirst;
  }
}
